   \section{Cache coherence protocols}

   A program running on multiple processors will normally have \textbf{multiple copies} of the same data in several caches. In a coherent multiprocessor the caches should provide both \textbf{migration} (movement of data) and \textbf{replication} (multiple copies of data) of shared data items.


